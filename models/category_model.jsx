import { View, Text, TextInput, StyleSheet , Color} from 'react-native'
import React from 'react'
import { Ionicons } from '@expo/vector-icons'
import { router } from "expo-router";

const Category = () => {
     
    const category_images = [
         require('./images')
    ];
}